spring:
  application:
    name: fitQuest
  datasource:
    url: jdbc:postgresql://localhost:5432/fitquest
    username: postgres
    password: shahbain
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type.descriptor.sql.BasicBinder: trace
